## Reports Folder

Generated quality and DB health outputs are stored under timestamped run folders:

- `quality-<timestamp>/`
- `db-health-<timestamp>/`

Reference artifacts that are not generated per run live in:

- `reference/`

Recommended hygiene:

- Keep recent runs for active debugging.
- Archive or prune older runs periodically.
