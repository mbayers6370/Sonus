{
  "name": "language-app-project",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix sonus-react run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:all": "bash -lc 'trap \"kill 0\" EXIT; npm --prefix backend run dev & npm --prefix sonus-react run dev & wait'",
    "checklist": "node scripts/regression-checklist.mjs",
    "test:core": "npm --prefix backend run test:core"
  }
}
